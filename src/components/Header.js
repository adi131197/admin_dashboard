import React from "react";
import "./Header.css";

const Header = () => {
  return (
    <div className="header">
      <span className="header-admin">Admin</span>UI
    </div>
  );
};

export default Header;
